# Deferred Register

Living document per `CLAUDE.md` §0/§3.1: every unverified claim (`V-xx`) and deliberate
debt/postponement (`D-xx`) discovered during implementation lives here, ranked by
consequence within each list. Created in the same pass as
`docs/tasks/TASK-scaffold-monorepo.md`, the first task to postpone anything.

Update this file in the same pass that resolves an item — move it to "Resolved" with the
date and what was decided, don't just delete it.

## V-xx — Unverified claims

| ID | Claim | Where it matters | Re-evaluation trigger |
|----|-------|-------------------|------------------------|
| V-3 | `libvpx-vp9`'s `-b:v 0` flag is required alongside `-crf` for ffmpeg to treat VP9 encoding as true constant-quality mode — without it `-crf` is claimed to be silently ignored in favor of the encoder's default bitrate-target mode (`TASK-video-audio-processors.md`). Confirmed via `ffmpeg -h encoder=libvpx-vp9` (flag exists, range 0–63) and corroborating secondary sources, but **not** against ffmpeg's own canonical trac wiki (`trac.ffmpeg.org/wiki/Encode/VP9`), which returned an access-denied/bot-challenge page when fetched for this task rather than a 404. | `workers/internal/processors/ffmpeg.go`'s `videoCrfArgs` for `libvpx-vp9` — if wrong, `video.transcode`/`video.compress` to `webm` silently produce larger-than-intended files rather than erroring, since ffmpeg wouldn't reject the flag combination either way. | Re-fetch `trac.ffmpeg.org/wiki/Encode/VP9` directly (or an equivalent primary source) once reachable, or before `video.compress`'s output-size behavior is depended on by anything user-facing (e.g. a "target file size" editor control). |
| V-4 | `workers/Dockerfile`'s runtime stage actually contains a working `ffmpeg` binary and a correctly linked `libvips42`, and the built image runs as the non-root `worker` user (`TASK-ci-worker-image.md`). The image built successfully (`docker build` completed, `docker images` showed the ~207MB result) and every package/version claim in the task doc was checked in isolation (`apt-cache policy`/`apt-cache search` inside fresh `ubuntu:24.04`/`debian:bookworm(-slim)` containers, `docker run ... which gcc cc pkg-config` against the exact builder tag). What was **not** done: actually running the built `plexus-worker:test` image and checking `whoami`, `which ffmpeg`, and `ldconfig -p \| grep vips` inside it — the local Docker daemon hit `ENOSPC` mid-verification, and after the disk was freed the daemon got stuck mid-VM-recovery and never came back up in-session. | Whether the container is actually runnable as shipped — a broken `ENTRYPOINT`, missing runtime lib, or permissions issue on `/data/worker-output` would only surface here, not in `docker build`. | Before this image is used for anything beyond a local `docker build` smoke test (pushed to a registry, run in any deploy target) — run `docker run --rm --entrypoint bash workers -c "whoami && which ffmpeg && ldconfig -p \| grep vips"` once Docker is available again. |
## D-xx — Deliberate debt / postponements

| ID | Decision | Why deferred | Re-evaluation trigger |
|----|----------|---------------|-------------------------|
| D-1 | `proto/` (gRPC plugin contract) and `packages/` (shared TS types) are still **not** scaffolded, even though CLAUDE.md §4 proposes them as part of the eventual layout. The `apps/web` portion closed 2026-08-06 (`TASK-editor-scaffold.md`) — scaffolded via `pnpm create next-app@latest`. The Edit Recipe schema landed 2026-08-07 (`TASK-recipe-schema.md`) but **inside `apps/web`**, not `packages/` — see new `D-17`. | Phase 1 (spec's phasing) only needed the orchestrator and one Go worker type; Phase 2 (Editor MVP) needs `apps/web`, which is why that portion closed now. `proto/`/`packages/` still have no consumer — scaffolding them early risks drifting stale (framework/tooling versions moving on, per CLAUDE.md §2.0) before Phase 3/4 actually touch them. The recipe schema has only one consumer (`apps/web`) until Phase 3 wires it into the orchestrator for "Apply to Batch," so it didn't trigger `packages/` yet either — see `D-17`. | `proto/` at Phase 4 start (or earlier if Phase 3's "Apply to Batch" needs the recipe/pipeline contract formalized sooner). `packages/`: Phase 3, when `apps/orchestrator` needs to read/write recipes for Apply to Batch — at that point `RecipeStep`/`Recipe` moves from `apps/web/src/lib/recipe/` into `packages/` and both apps import the same definition instead of the two hand-kept-parallel shapes tracked in `D-17`. |
| D-2 | Root `package.json`'s `packageManager` field is pinned to `pnpm@11.5.2` (what was actually installed locally via nvm/corepack at scaffold time), not the `pnpm@11.14.x` line identified as current-stable during CLAUDE.md §2.0 research. | Both are the same major (pnpm 11) with no workspace-schema-breaking differences found during scaffolding; forcing a corepack download of a newer patch mid-scaffold added risk for no functional benefit. | Low urgency — revisit opportunistically, or immediately if a pnpm 11.14+-only feature (e.g. `pnpm doctor`, `pnpm lane`) becomes useful. |
| D-3 | Object storage choice (self-hosted MinIO vs. managed S3-compatible) — spec §"Open Questions," not resolved by this scaffold. | Not needed until Phase 3 (presigned upload flow) per spec's phasing; Phase 1's linear pipelines don't require object storage wiring yet. | Start of the Phase 3 task doc that implements presigned upload. |
| D-4 | Auth approach (reuse existing patterns vs. OAuth device flow) — spec §"Open Questions," not resolved by this scaffold. | Phase 1 (orchestrator + worker + Postgres + NATS, linear pipelines) has no user-facing auth surface yet per the spec's phasing. | Before any orchestrator endpoint needs to be gated — likely Phase 3 (realtime/SSE to specific users) or whenever multi-user access is first exercised. |
| D-5 | Plugin sandboxing tier (gRPC-only for v1 vs. pulling WASM/`wazero` into P0) — spec §"Open Questions," leaning P2 per the spec itself. | No plugin work happens before Phase 4. | Once the gRPC plugin contract (`proto/`) plumbing exists and a second/untrusted plugin author scenario is concretely being designed for (per the spec's own note: "worth revisiting once gRPC plumbing is done"). |
| D-7 | Pipeline definitions are accepted as JSON only (`CreatePipelineDto` in `apps/orchestrator/src/pipelines/dto/create-pipeline.dto.ts`); the spec's Core Concepts example is YAML, and no YAML parsing layer exists yet (`TASK-job-state-machine.md`). | YAML is a thin parsing layer on top of the same DTO shape and isn't needed to prove the job state machine or linear-DAG validation, which is this task's actual scope. | Whichever task first needs the CLI/UI pipeline-authoring path (spec's "power user... define a custom multi-step pipeline in YAML" user story) — likely a Phase 3 UI task, or sooner if YAML presets are wanted earlier. |
| D-8 | `StepDispatchMessage`/`StepResultMessage` are hand-duplicated as plain JSON shapes on both sides of the NATS boundary: `apps/orchestrator/src/jobs/dispatch-message.ts` (TS) and `workers/internal/dispatch/message.go` (Go), with no shared/generated contract (`TASK-nats-job-dispatch.md`). | `proto/` (the intended single source of truth for cross-language contracts, per CLAUDE.md §4) isn't scaffolded until Phase 4 per D-1. Standing up `proto/` + `buf` codegen just for two small structs is infrastructure work orthogonal to proving the dispatch loop. | When `proto/` is scaffolded at Phase 4 (D-1's own trigger), fold these two message shapes into it. Re-evaluate sooner if the shapes actually drift out of sync and cause a bug — the hand-duplication is the risk this note exists to track. |
| D-9 | `plexus.jobs.dispatch` is one flat subject for every built-in processor type — no per-processor subject fan-out (e.g. `plexus.jobs.dispatch.image.resize`), even though the spec's Core Concepts allows arbitrary processor ids (`TASK-nats-job-dispatch.md` §1). | Phase 1 is explicitly "single Go worker type" per the spec's phasing — one worker pool already consumes everything, so per-processor routing has no consumer to route to yet. | Once external plugins (Phase 4, alongside D-1's `proto/` and D-5's plugin sandboxing) need different processor types routed to different consumers/languages. |
| D-10 | The `nats` npm package (v2.x) is deprecated — `pnpm add nats` resolves and installs fine but npm flags "Package moved. Use @nats-io/transport-node." The client was split into scoped `@nats-io/*` packages (`transport-node`, `jetstream`, `nats-core`, `kv`, `obj`) for v3 (`TASK-nats-job-dispatch.md`). | Not deferred work — recorded so a future search for "the nats package" doesn't land on the deprecated one again. | None — already resolved; kept as a note for discoverability. |
| D-11 | `image.resize`/`image.convert`/`image.compress` read `inputRef` and write `outputRef` as literal local filesystem paths under `WORKER_STORAGE_DIR` (`workers/internal/processors/output.go`), not object storage (`TASK-builtin-processors.md`). | D-3 (object storage self-hosted vs. managed) isn't resolved until Phase 3's presigned-upload work; this task only needs processors to actually run, not a real upload/download flow. The interface (`Func` returns an opaque `outputRef` string) is shaped so swapping in real MinIO/S3 later is localized to `output.go`, not a redesign. | Start of the Phase 3 task doc that implements presigned upload — `output.go`'s local read/write becomes a download-to-temp/upload-result pair there. |
| D-13 | `image.resize` requires both `width` and `height`; single-dimension resize (scale by one dimension, preserve aspect) is not supported (`TASK-builtin-processors.md`). | govips's `Thumbnail`/`ThumbnailWithSize` API takes both dimensions; the idiomatic way to get single-dimension resize (an artificially large value for the unconstrained dimension) is a real pattern for other libvips bindings but wasn't verified against this specific govips version, and CLAUDE.md §0 forbids inventing library behavior. | Before any editor/pipeline UI needs "resize by width only" — `[VERIFY]` the oversized-dimension idiom against govips's own tests/docs first, or find an official single-dimension API, before implementing. |
| D-15 | `video.transcode`/`video.compress` support exactly `mp4` (h264/aac) and `webm` (vp9/opus); `audio.extract`/`audio.convert` support exactly `mp3`/`aac`/`opus` (+`wav` for convert). No h265, av1, flac, or alac (`TASK-video-audio-processors.md`). | Same reasoning as the four fixed image formats in slice 3 — these are the pairs the spec's P0 line and examples actually need, not ffmpeg's full encoder surface (CLAUDE.md's "no code for hypothetical requirements"). | Whenever a concrete use case needs another codec — additive change to `videoCodecsForContainer`/`audioCodecForFormat` in `workers/internal/processors/ffmpeg.go`, not a redesign. |
| D-16 | `workers/Dockerfile` builds a worker image (`TASK-ci-worker-image.md`), but nothing pushes it to a registry or wires it into any deploy pipeline — no `docker build`/`push` step in `ci.yml`, no registry chosen. | Choosing a registry/deploy target is its own decision (comparable in shape to the spec's still-open object-storage question, D-3) and wasn't needed to prove CI itself. | Whenever Phase 1 moves from "runs locally / in CI" to "deployed somewhere" — needs a registry decision first. |
| D-17 | The Edit Recipe type (`apps/web/src/lib/recipe/schema.ts`, Zod) is hand-kept parallel to the orchestrator's pipeline step shape (`StepDto`/`PipelineStepDefinition` in `apps/orchestrator/src/pipelines/dto/create-pipeline.dto.ts` and `apps/orchestrator/src/db/schema.ts`, `class-validator`) rather than sharing one definition — two different files, two different validation libraries, in two different languages (`TASK-recipe-schema.md`). Same category of debt as `D-8`'s NATS message duplication. | `packages/` isn't scaffolded yet (`D-1`) and the recipe schema has only one real consumer today (`apps/web`) — Phase 2 has no orchestrator/batch integration. Recipe params are also typed per-processor (a discriminated union) rather than `StepDto`'s untyped `Record<string, unknown>`, a deliberate divergence for the live-preview and composite-slider work, not something a naive shared type could paper over without also changing the orchestrator's validation. | Phase 3, when `apps/orchestrator` needs to consume editor recipes for "Apply to Batch" (same trigger as `D-1`'s `packages/` update) — promote `apps/web/src/lib/recipe/schema.ts` into `packages/` and have both apps import it. Re-evaluate sooner if the two shapes actually drift and cause a bug. |
| D-19 | Composite-slider mapping (`TASK-composite-slider-mapping.md`) decided four new processor ids — `image.adjustLight`, `image.adjustColor`, `image.blackAndWhite`, `image.sharpen` — with a P0 param subset (`exposure`/`brightness`/`contrast`/`blackPoint`, `saturation`, `intensity`/`neutrals`/`tone`, `intensity`) each mapped to a concrete govips call. The Zod schema slice landed (`TASK-composite-processors-schema.md`). The Go processor slice landed (`TASK-composite-processors-go.md`): all four ids are implemented under `workers/internal/processors/` and registered in `registry.go`. The preview-shader slice landed (`TASK-composite-preview-shaders.md`): both `WebGPURenderer`/`WebGL2Renderer` now run an ordered multi-pass pipeline over `recipe.steps` (`apps/web/src/lib/preview/color-math.ts` is the shared reference math), so all four composite sliders render live instead of no-op'ing — manually verified in a real browser (both a real WebGPU adapter and the WebGL2 fallback path). `V-2`-style numeric drift measurement per control landed (`TASK-recipe-fidelity-drift.md`, 2026-08-07) — see the resolved `V-2` entry below. The real curated-slider editor UI (`D-6`) landed (`TASK-editor-composite-ui.md`, 2026-08-07): `apps/web/src/app/editor/page.tsx` replaces the raw-per-parameter demo as the primary surface, with a single "Light" master slider (blend ratios in `apps/web/src/lib/editor/light-blend.ts`) fanning into `exposure`/`brightness`/`contrast`/`blackPoint`, direct Color/B&W/Sharpen controls (no invented fan-out — Apple's own panes don't have one there either), and `useRecipeHistory` (`apps/web/src/lib/editor/history.ts`) backing undo/redo. `apps/web/src/app/preview-demo/page.tsx` stays as the renderer smoke-test harness. | The design pass (`TASK-composite-slider-mapping.md`) was scoped to decide *which* parameters and *how each maps to Go*, not to implement them. The schema slice (`TASK-composite-processors-schema.md`) encoded that decision into the type both the future editor UI and preview shader need to agree on. The Go slice (`TASK-composite-processors-go.md`) makes the four processor ids actually dispatchable — `BlackAndWhite`'s three params required a real composition decision (single `Recomb`-based grayscale rather than a `ToColorSpace(BW)` + `Recomb` double-grayscale) that the mapping doc explicitly left open, and `AdjustLight`'s `blackPoint=1.0` needed a divide-by-zero guard the mapping doc's formula didn't anticipate. The shader slice chose true ordered multi-pass rendering (recipe-step order, not a hardcoded Light→Color→B&W→Sharpen sequence) over a cheaper fixed-order single shader, because only the ordered version satisfies "recipe/pipeline unification, no translation step" for recipes that don't originate from the editor's own fixed slider layout (e.g. hand-authored batch YAML). | Resolved — see new `D-22` for what the UI task itself deferred (blend-ratio tuning against real photos, crop, presets). |
| D-18 | The live preview renderer (`apps/web/src/lib/preview/`) treats `image.convert`/`image.compress` recipe steps as a **visual passthrough** — no shader attempts to simulate JPEG/WebP/AVIF quantization artifacts or format-conversion appearance per-frame (`TASK-preview-renderer.md`). Only `image.resize` gets real geometric preview treatment. | Format is a container choice and `quality` only affects lossy-encoding artifacts that exist after a real encode pass — simulating that live would mean inventing codec/browser behavior CLAUDE.md §0 already warns against, for a feature (visible "JPEG quality" live preview, à la Photoshop's "Save for Web") nobody has asked for; Apple Photos itself doesn't expose it either. | If a future editor-UX task (distinct from `D-6`'s composite sliders) wants users to see compression artifacts live — e.g. a "file size vs. quality" control — needing either a debounced real-time re-encode preview or an approximated-artifact shader, neither attempted here. |
| D-20 | `apps/web/src/lib/preview/color-math.ts`'s `applyAdjustLight()` (and both renderers' `image.adjustLight` shader pass) applies its affine transform to RGB only, leaving alpha unchanged — but `workers/internal/processors/adjust_light.go`'s `Linear1` calls apply uniformly to *every* band, alpha included, with no exemption in the Go code (`TASK-composite-preview-shaders.md`). | Invisible for the opaque photos this editor mostly handles (constant alpha=1 stays 1 either way). For a genuinely transparent input, Go's behavior shifts transparency as a side effect of brightness/contrast edits — replicating that in the live preview would make translucent images visibly fade in/out as the Light slider is dragged, a worse and more confusing user-facing bug than the RGB-only/alpha-exempt divergence from Go. | Before this editor supports transparent-source images as a real, tested case — decide then whether to fix the Go processor (exempt alpha from `Linear1`, changing its output) or make the preview replicate Go's current alpha-mutating behavior; not decided here since neither side of that trade-off has been chosen yet. |
| D-21 | `image.resize`'s live-preview geometry (`apps/web/src/lib/preview/geometry.ts`, `TASK-preview-renderer.md`) is still applied only as the *final* blit's UV-rect crop/scale, regardless of where `image.resize` actually sits in `recipe.steps` — unchanged by `TASK-composite-preview-shaders.md`'s new ordered multi-pass pipeline, which runs every `image.adjustLight`/`adjustColor`/`blackAndWhite`/`sharpen` step in real recipe order but always against the full original-resolution source texture. | A real resampling pass (change canvas dimensions mid-pipeline, matching Go where resize is just another step) is meaningfully more architecture than this task's scope justified — the composite-adjustment ordering (the concrete problem the mapping doc flagged, B&W-after-Color) is now correct; resize's *position* relative to those steps is a narrower, lower-probability case (content ops are resolution-relative only for `image.sharpen`, where a resize-before-vs-after ordering changes the effective sharpen radius). | If `V-2`'s drift measurement (or real user reports) shows resize-position-dependent preview/export mismatches for recipes mixing `image.resize` with `image.sharpen` — turn `image.resize` into a genuine resampling pass in the ordered pipeline instead of a post-hoc geometry step. |
| D-23 | The live preview's `image.resize` (`apps/web/src/lib/preview/{webgpu,webgl2}-renderer.ts`'s final blit pass, `apps/web/src/lib/preview/geometry.ts`) resamples with hardware bilinear texture sampling, while Go's export (`workers/internal/processors/resize.go`'s `img.Thumbnail()`) resamples with Lanczos3 — a confirmed, real algorithmic mismatch (`TASK-resize-drift.md`, resolved `V-6`), not a rounding-mode gap. Measured drift (`apps/web/src/lib/preview/drift.test.ts`, `describe('image.resize (V-6)')`) is an order of magnitude worse than the four composite controls: worst observed `resize-inside-upscale` (1.5× upscale) mae=7.92, max=99.25/255, meanΔE=3.70; `RESIZE_BOUNDS = { mae: 11.0, max: 130, meanDeltaE: 5.0 }`. No shader-side Lanczos3 approximation was attempted. | A real Lanczos3 (or Lanczos-ish) approximation in a fragment shader is materially more complexity than a single hardware-sampler texture fetch — a multi-tap kernel (typically 6×6 or wider for Lanczos3) evaluated per output pixel, with no existing precedent anywhere in this codebase's shaders. Building it before a measured number proved bilinear insufficient would be exactly the "code for hypothetical requirements" CLAUDE.md argues against — this task's job was to measure and document, which it now has. | If a real user-visible complaint about resize preview/export mismatch comes in (e.g. "the exported thumbnail looks noticeably different from what I saw while dragging the crop/resize box"), or before any editor UX explicitly promises pixel-close resize preview fidelity — implement a multi-tap Lanczos-ish shader pass then, informed by these now-measured baseline numbers. |
| D-22 | `TASK-editor-composite-ui.md`'s Light blend ratios (`apps/web/src/lib/editor/light-blend.ts`) are a first-pass judgment call (`exposure = t*1.5`, `brightness = t*0.3`, `contrast = t*0.25`, `blackPoint = max(0,-t)*0.3`) picked without dragging against real photos — the task doc itself scoped this as "answered by dragging a slider against real photos and eyeballing the curve," which wasn't done here (no image fixture / visual review loop was available in this pass). Also out of scope for that task: **crop** (spec's P0 editor bullet groups it with light/color/filter, but no `image.crop` processor/schema/Go implementation exists at all — not invented here) and **presets** ("pick a look" before blank editor, P1 in the spec, its own task). `TASK-highlights-shadows-preview-parity.md` (resolved `D-25`) folded one more question into this same scope: `applyLightBlend` still hardcodes `highlights: 0, shadows: 0` for the master slider — those two params are reachable today only via the manual "Adjust manually" sliders `LightControl.tsx` gained in that task, not via the single master blend ratio. | Blend-ratio tuning needs a real visual feedback loop (drag, look, adjust) that a single implementation pass without image review can't substitute for with any real confidence — shipping a documented, clamped, unit-tested starting formula was preferable to blocking the rest of the UI (history, disclosure pattern, direct controls) on it. Crop and presets are each a real processor/schema/Go slice (crop) or a separate P1 feature (presets), not a UI-only addition to this task. Folding highlights/shadows into the master blend ratio is the same "judgment call needing a real visual feedback loop" as the original four, not a new kind of question — bundling it into a rendering-focused task would have buried a UI decision inside shader work. | Blend ratios (including whether/how to add highlights/shadows to them): revisit the first time a real photo is dragged through `/editor` and the Light slider's feel is actually judged, per the task doc's own scoping — likely as a quick follow-up, not a new task doc. Crop: needs its own `TASK-*` starting from a new `image.crop` processor (Go + schema), same shape as `TASK-composite-slider-mapping.md`'s process. Presets: Phase 2/3 boundary per spec phasing, own task doc. |
| D-24 | `workers/internal/govips-fork/vips/` is a local, hand-maintained fork of `github.com/davidbyttow/govips/v2`'s `vips` subpackage (v2.18.0 base, wired in via `workers/go.mod`'s `replace github.com/davidbyttow/govips/v2 => ./internal/govips-fork`), adding the `Tonelut` constructor V-7 found missing upstream (`TASK-highlights-shadows-tonelut.md`). Only `vips/` itself was copied (752KB) — not `resources/`/`assets/`/`examples/`/`cmd/`, and its own `*_test.go` files were deleted (not needed to build the replace target, and would have pulled in `testify` et al. for no benefit). | A fork was the only option once investigated: `Tonelut` is a from-scratch LUT generator (no input `*ImageRef`), and `ImageRef.image`/`newImageRef` are both unexported, so no wrapper outside the `vips` package itself can construct the resulting `*ImageRef` — unlike `Sharpen`/`Recomb`/`Maplut`, which mutate an existing `r.image` and so could (hypothetically) be wrapped externally if they weren't already exported. | Any future `govips` upgrade needs this ~30-line diff (`tonelut.go`) manually reapplied to the new version's `vips/` before bumping `internal/govips-fork/go.mod`'s copied source — there's no automation for this today. Drop the fork entirely (and the `replace` directive) if a future govips release exports `Tonelut` itself. |
| D-28 | **Grain** (`image.blackAndWhite.grain`) needs a small `workers/internal/govips-fork/vips/` extension before any Go processor work can start: `vips_gaussnoise` exists at govips' C-binding layer (`vips/generated.go`, `vipsGenGaussnoise`) but — confirmed by grepping every `vips/image_*.go` file in the full `v2.18.0` source — has no public `ImageRef`-returning wrapper anywhere in the package, per `TASK-vibrance-cast-grain-spike.md` (resolved `V-8`). Same root cause as `D-24`'s `Tonelut` gap: a from-scratch generator with no input `*ImageRef`, and `newImageRef`/`ImageRef.image` both unexported, so no wrapper *outside* the `vips` package can construct the result. | Direct precedent already exists in this codebase (`D-24`'s `tonelut.go`, ~30 lines) — this isn't new research, it's the same fix applied to a second missing wrapper. Compositing the resulting noise image onto the source (via `Composite`/`BlendMode`) uses primitives that are already fully public — only the noise-image constructor itself needs the fork. | Start of the `image.blackAndWhite.grain` implementation task — add `vips.NewGaussnoiseImage(width, height int, opts *GaussnoiseOptions) (*ImageRef, error)` to the fork mirroring `tonelut.go`'s shape, then wire `Composite` in `workers/internal/processors/black_and_white.go`. |
| D-29 | **Vibrance**'s exact boost curve and **Grain**'s blend-mode/intensity-to-sigma mapping are both aesthetic judgment calls, not primitive gaps — per `TASK-vibrance-cast-grain-spike.md` (resolved `V-8`). Vibrance is buildable (`ToColorSpace(LCH)` → extract chroma band → nonlinear `Multiply`/`Add`/`Linear` boost → `BandJoin` back), but unlike the four Light params (pinned to libvips' own `tonelut.c`) or Cast's grey-world math (`D-27`, textbook-exact), there is no single documented-correct "vibrance" formula anywhere primary-sourced — Adobe's and Apple's are both proprietary/unpublished. Grain's `BlendMode` choice (`Overlay`/`SoftLight`/`Add` are all real govips options) and sigma mapping are the same category. | Inventing a specific curve or blend mode now to make this look "resolved" would be exactly the unverified-claim problem CLAUDE.md §0 exists to prevent, and would ship a curve nobody has looked at against a real photo — same situation `D-22` already identified for the Light master slider's blend ratios. | Same trigger as `D-22`: the first time a real photo is dragged through `/editor` and these controls' feel is actually judged — bundle as a follow-up to that same visual-tuning pass, not a separate research task. |
| D-26 | `workers/Dockerfile` was edited to `COPY internal/govips-fork ./internal/govips-fork` before `RUN go mod download` (previously only `go.mod`/`go.sum` were copied at that stage) — required because `go mod download` needs to read a local `replace` target's directory (`D-24`) directly, unlike a normal registry module. Not verified end-to-end: **local Docker daemon is unavailable in this environment** (same gap as `V-4` — `docker build` fails immediately with "Cannot connect to the Docker daemon"), so the actual image build with this change has not been run. | `V-4` already tracks this environment's inability to run/verify the worker image; this is an additional untested-since-`V-4` change layered on the same gap, not a new independent problem. | Same trigger as `V-4`: before this image is used for anything beyond a local smoke test, run `docker build -t plexus-worker:test workers/` once Docker is available and confirm the build actually completes with the new `COPY` step (not just that the daemon connects). |

## Resolved

| ID | Was | Resolved | Date |
|----|-----|----------|------|
| D-30 | `image.adjustColor.castStrength` (D-27's grey-world white-balance correction, implemented Go-side in `TASK-adjust-color-cast.md`) had no live-preview shader pass and no editor UI control — same two-phase split `V-7`→`D-25` already used for `image.adjustLight`'s `highlights`/`shadows`. The open design question: grey-world needs a whole-image per-band mean, a full-image reduction no existing shader in either renderer performs — every other composite control is a pure per-pixel function. | `TASK-color-cast-preview-parity.md` closed the gap with a GPU-side iterative 2×2 box-downsample pyramid: a new pipeline/program (`MEAN_DOWNSAMPLE_WGSL`/`MEAN_DOWNSAMPLE_FRAGMENT_SHADER_SOURCE`, `textureLoad`/`texelFetch`-based, no sampler) repeatedly halves a texture (`geometry.ts`'s new `computeMeanChainSizes`, `ceil(size/2)` per step) until it reaches 1×1, entirely inside the same synchronous, single-command-encoder execution model every other pass already uses — chosen specifically to avoid an async GPU→CPU readback, which no renderer method (`init`/`render`/`dispose`) has today and which would stall or restructure the render loop for one control. The chain runs unconditionally on every `image.adjustColor` step (cheap next to the per-pixel passes already run every frame; the shader's scale factor is an exact `1.0` at `castStrength=0` regardless of what the mean texture holds, so no bind-group-shape branching was needed). This pyramid is **not** an exact arithmetic mean on non-power-of-two images — each halving step duplicates the last texel to pair up an odd count, biasing edge texels slightly high — a bounded preview approximation like every other control in `color-math.ts`, not a new unverified claim. `color-math.ts`'s CPU reference (`applyAdjustColor`, extended with an optional `mean` param) instead computes the exact mean via a new `computeMeanRGB`, since the pure-TS reference has no reason to replicate the GPU's approximation. `ColorControl.tsx` got a direct `Cast` slider (no "Adjust manually" fold — matches `BlackAndWhiteControl.tsx`'s precedent for a curated raw-param set with no fan-out master blend, not `LightControl.tsx`'s fold-behind-a-blend pattern). Drift measured: two new isolated `workers/cmd/gendriftgolden` points (`color-cast-0.5`, `color-cast-1.0`, `saturation=0`) regenerated against real govips output (libvips 8.18.5) — existing golden fixtures came back byte-identical. Measured drift on the CPU-reference path: mae≈0.000016, max≈0.0001, meanΔE≈0.000007 — effectively bit-exact, since `computeMeanRGB` matches govips's `Average()` exactly and both sides run the identical linear-scale formula; `COLOR_BOUNDS` unchanged. This only measures `color-math.ts` against Go, **not** the GPU renderers' box-pyramid approximation — no automated test exercises actual WGSL/GLSL execution in a browser (same limitation `D-25` already documented; the pyramid's non-power-of-two bias remains unverified against a real image with odd dimensions in an actual browser). | 2026-08-07 (`TASK-color-cast-preview-parity.md`) |
| D-27 | **Cast** (`image.adjustColor.castStrength`) was unblocked — the grey-world white-balance algorithm (`Stats`/`Average` for per-channel means, `Linear` with per-band coefficients to equalize toward gray) was buildable from govips primitives already used elsewhere in this codebase, per `TASK-vibrance-cast-grain-spike.md` (resolved `V-8`). Only the `castStrength` blend-amount was an open UX call (`0.0..1.0` lerp). | `TASK-adjust-color-cast.md` implemented it: `apps/web/src/lib/recipe/schema.ts`'s `adjustColorParamsSchema` gained `castStrength: 0.0..1.0`, optional/defaulted to `0.0` (same backward-compat pattern as `highlights`/`shadows`, `V-7`). `workers/internal/processors/adjust_color.go`'s new `applyCast` reads per-band R/G/B means via `ExtractBandToImage`+`.Average()` (not `Stats()`+`GetPoint`, see below), computes the target as their mean, and scales each band toward it via `img.Linear(a, b)`, blended by `castStrength`; requires ≥3 bands (errors on grayscale input). Two real gotchas found by testing against actual govips v2.18.0/libvips 8.18.5 before writing the processor, not assumed from D-27's summary: (1) `Stats()` mutates the image into a 1-band stats image whose per-band values, read via `GetPoint`, only come back correct because the public wrapper's hardcoded 3/4-element read happens to be zero-padded past the real 1-band result — undocumented C-level behavior, not a guaranteed contract, so `ExtractBandToImage`+`Average` was used instead (same result, no reliance on that behavior); (2) `vips_linear`'s coefficient vector must have length 1 or exactly `Bands()` — confirmed via a real error (`"linear: vector must have 1 or 4 elements"` from a 3-element vector against a synthesized 4-band RGBA fixture) — so the processor builds a full `Bands()`-length vector with an identity (`1.0`) coefficient for alpha. Grey-world math verified end-to-end on `testdata/images/gradient.jpg`: `castStrength=0` is an exact no-op; `castStrength=1` converges per-band means from a spread of ~31.5 to ~0.17 after a real JPEG re-encode round-trip. Preview shader (`webgpu-renderer.ts`/`webgl2-renderer.ts`) and editor UI (`ColorControl.tsx`) intentionally not included — see new `D-30`. | 2026-08-07 (`TASK-adjust-color-cast.md`) |
| V-8 | No govips primitive was found (source read, `vips/image_color.go`/`image_pixel.go`/`image_transform.go`, 2026-08-07) for **Vibrance** (nonlinear, saturation-dependent boost — distinct from `Modulate`'s uniform saturation), **Cast** (grey-world/white-balance correction), or **Grain** (noise synthesis). Absence-of-evidence, not confirmed impossible — a full govips API surface search wasn't exhaustive (`TASK-composite-slider-mapping.md`). | `TASK-vibrance-cast-grain-spike.md` ran the exhaustive search against the full `github.com/davidbyttow/govips/v2@v2.18.0` module-cache source (not just the ~30% copied into `workers/internal/govips-fork/vips/`), and found the three params are not actually the same kind of gap: **Cast** is fully unblocked today (`Stats`/`Average`/`Linear` implement the textbook grey-world algorithm exactly — no fork, no formula invention needed; only the blend-amount UX is undecided, see new `D-27`). **Grain** is blocked on real, scoped infrastructure with a direct precedent already in the codebase: `vips_gaussnoise` exists at the C-binding layer (`vipsGenGaussnoise` in `vips/generated.go`) but has no public `ImageRef` wrapper anywhere in the package — same shape of gap `D-24` already solved for `Tonelut`, same fix applies (see new `D-28`). **Vibrance**'s curve (and Grain's blend-mode/intensity feel) genuinely has no primary-source-correct answer to pin down — Adobe's and Apple's algorithms are both unpublished/proprietary — so unlike the other two, that piece stays deliberately unresolved as a visual judgment call, folded into `D-22`'s real-photo tuning pass rather than invented here (see new `D-29`). Confirmed no fabricated formula was introduced anywhere in this resolution, per CLAUDE.md §0. | 2026-08-07 (`TASK-vibrance-cast-grain-spike.md`) |
| D-25 | `image.adjustLight`'s `highlights`/`shadows` params were implemented and tested Go-side only (`TASK-highlights-shadows-tonelut.md`, resolved `V-7`) — the live preview rendered them identically to `highlights=0, shadows=0`, and the editor UI had no sliders for either. | `TASK-highlights-shadows-preview-parity.md` closed the gap. libvips' `tonelut` curve was pinned to primary source (`libvips/create/tonelut.c`): with its default `Lb/Lw/Ps/Pm/Ph` (0/100/0.2/0.5/0.8, unchanged by `adjust_light.go`), the derived positions `Ls=20, Lm=50, Lh=80` give `tone_curve(x) = x + S*shad(x) + H*high(x)`, and the four-branch Hermite `shad`/`high` bumps factor exactly into `smoothstep(Lb,Ls,x)-smoothstep(Ls,Lm,x)` / `smoothstep(Lm,Lh,x)-smoothstep(Lh,Lw,x)` (algebraically verified at every branch boundary) — both WGSL and GLSL ES 300 provide `smoothstep()` as a builtin, so no lookup table was needed. Landed in `apps/web/src/lib/preview/color-math.ts` (new exported `highlightsShadowsL`, `applyAdjustLight` runs it in Lab space after the existing RGB chain, skipping the Lab round-trip entirely when both params are `0`), both renderers (`ADJUST_LIGHT_WGSL`/`ADJUST_LIGHT_FRAGMENT_SHADER_SOURCE` gained the Lab helpers + curve function; the uniform-passing helpers — `encodeUniformPass`, `runContentPass`/`ContentProgram` — generalized from a fixed 4-float `vec4` to N chunks, since 6 params no longer fit one), and `LightControl.tsx` (new manual Highlights/Shadows sliders; the master "Light" blend slider deliberately still doesn't reach them — see updated `D-22`). Also fixed a real bug found in the same pass, not part of the original plan: `apps/web/src/app/editor/page.tsx`'s `deriveRecipe` only emitted an `image.adjustLight` step when the *original* four params were nonzero, so adjusting only Highlights/Shadows from identity would have silently produced no recipe step at all. Drift measured, not assumed: two new isolated `workers/cmd/gendriftgolden` points (`light-shadows-strong`: `shadows=0.7`; `light-highlights-strong`: `highlights=0.7`, both otherwise identity) regenerated against real govips output (libvips 8.18.5, confirmed locally installed) — existing golden fixtures came back byte-identical, confirming no regression. Measured drift: `light-shadows-strong` mae=0.092/max=0.796/meanΔE=0.043, `light-highlights-strong` mae=0.218/max=0.892/meanΔE=0.123 — both comfortably under the existing `LIGHT_BOUNDS` (mae 0.6/max 1.5/meanΔE 0.4) set by the original two points, so the bound is unchanged. | 2026-08-07 (`TASK-highlights-shadows-preview-parity.md`) |
| V-7 | `github.com/davidbyttow/govips`'s generated set (`vips/generated.go`) contains internal `vipsGenTonelut`/`vipsGenHistLocal` bindings (confirming libvips itself exposes `tonelut`/`hist_local`), but neither has a public `(*ImageRef)` wrapper method — only `Maplut`, `Sharpen`, `Recomb` are exported. libvips maintainers' own recommended shadows/highlights-recovery approach ([discussion #4036](https://github.com/libvips/libvips/discussions/4036)) is a `tonelut`→`maplut` pipeline on the LABS L channel, blocking `image.adjustLight`'s `highlights`/`shadows` params (`TASK-composite-slider-mapping.md`). | Re-confirmed still true at `v2.18.0` (latest release — no newer version exists). Root cause is deeper than "missing wrapper": `tonelut` takes no input image (unlike `Sharpen`/`Recomb`/`Maplut`), and `ImageRef.image`/`newImageRef` are both unexported, so no wrapper *outside* the `vips` package can construct the resulting `*ImageRef` — only a same-package addition (local fork, `D-24`) works. `TASK-highlights-shadows-tonelut.md` added `workers/internal/govips-fork/vips/tonelut.go` (`Tonelut(opts) (*ImageRef, error)`, wired via a `replace` directive), implemented `image.adjustLight`'s `highlights`/`shadows` in `workers/internal/processors/adjust_light.go`, and schema'd them in `apps/web/src/lib/recipe/schema.ts`. Exact `tonelut` semantics (in_max/out_max/Lb/Lw/Ps/Pm/Ph/S/M/H) and LABS band-0 encoding (signed short, 0..32767 = logical L 0..100) were confirmed against libvips' own upstream source (`libvips/create/tonelut.c`, `libvips/colour/Lab2LabS.c`), not guessed. One real bug was found and fixed empirically during implementation, not predicted by the design doc: initially tried applying the LUT to the whole 3-band LABS image via `maplut`'s `band` option in a single pass, which corrupts the untouched a/b bands because `vips_maplut_build` relabels the *entire* output image's `BandFmt` as the LUT's format regardless of which band was mapped — verified via a failing test (positive `shadows` measurably *darkened* a fixture instead of brightening it) before landing the working `ExtractBandToImage`→`Maplut`→`BandJoin`→`CopyChangingInterpretation` sequence instead. Sign convention: `shadows` positive = brighter (matches libvips' own `S`); `highlights` positive = darker/recovered (Apple Photos/Lightroom convention, opposite of libvips' raw `H` — negated in code). Preview-shader/editor-UI parity intentionally not included — see new `D-25`. | 2026-08-07 (`TASK-highlights-shadows-tonelut.md`) |
| V-6 | Whether `image.resize`'s live preview (GPU hardware texture sampling) uses the same interpolation kernel as Go's export (`workers/internal/processors/resize.go`'s `img.Thumbnail()` → govips → `vips_thumbnail_image`) was unconfirmed — `[VERIFY: confirm govips/libvips' actual default Thumbnail interpolation kernel, likely Lanczos3, against libvips' own docs]`. | Confirmed directly from libvips' own source (`libvips/resample/{thumbnail,resize}.c`, `master`, fetched 2026-08-07): `vips_thumbnail_image`'s main-resize call passes no `"kernel"` argument, so it inherits `vips_resize`'s default — `vips_resize_class_init` registers `VIPS_ARG_ENUM(..., "kernel", ..., VIPS_KERNEL_LANCZOS3)` and `vips_resize_init` sets `resize->kernel = VIPS_KERNEL_LANCZOS3`. So Go export uses Lanczos3; the live preview uses hardware bilinear (`device.createSampler({magFilter:'linear',minFilter:'linear'})` in `webgpu-renderer.ts`, `gl.LINEAR` in `webgl2-renderer.ts`) — a real, different algorithm. `TASK-resize-drift.md` extended the `TASK-recipe-fidelity-drift.md` harness (`workers/cmd/gendriftgolden` gained a `resize` golden group at 3 points; `apps/web/src/lib/preview/drift.test.ts` gained a test-local `bilinearResample()` mirroring the renderers' actual blit-pass math) and measured real drift: worst case (`resize-inside-upscale`) mae=7.92, max=99.25/255, meanΔE=3.70 — roughly an order of magnitude worse than the near-bit-exact composite controls. Unlike those controls, this gap is now accepted debt rather than a bug to chase — see new `D-23`. | 2026-08-07 (`TASK-resize-drift.md`) |
| — | `apps/web` test runner choice, unverified pending checking Next.js's own current docs (`TASK-recipe-schema.md`). | **Vitest**, per `nextjs.org/docs/app/guides/testing/vitest` (confirmed current: page's own `version: 16.3.0` metadata (`lastUpdated: 2026-02-11`) matches this repo's installed Next.js exactly). Manual setup as documented: `vitest`, `@vitejs/plugin-react`, `vite-tsconfig-paths`, `jsdom`, `@testing-library/react`, `@testing-library/dom`; `vitest.config.mts` with the `jsdom` environment; `pnpm test` runs `vitest run` (non-watch, matching the orchestrator's CI-friendly convention in `.github/workflows/ci.yml`) rather than the doc's bare `vitest` (watch mode). | 2026-08-07 (`TASK-recipe-schema.md`) |
| — | TypeScript package manager choice (open per CLAUDE.md §4, "chosen at scaffold time") | **pnpm** — matches CLAUDE.md's own examples, first-class workspace support for `apps/*`+`packages/*` layout. | 2026-08-06 (`TASK-scaffold-monorepo.md`) |
| — | Go module import path pending confirmation of GitHub owner (`[VERIFY]` in `TASK-scaffold-monorepo.md`) | `github.com/benitopedro13/plexus/workers` — confirmed by user. | 2026-08-06 |
| — | pnpm's minimum supported Node.js major for the pinned v11 line (`[VERIFY]` in `TASK-scaffold-monorepo.md`) | pnpm 11 requires Node.js ≥22; repo pins `.nvmrc` to Node 24 (Active LTS, matches locally installed toolchain). | 2026-08-06 |
| — | Orchestrator data-layer / ORM choice, first picked in `TASK-job-state-machine.md` | **Drizzle ORM** (`drizzle-orm/node-postgres`), not Prisma. Initial plan was Prisma (NestJS's own documented recipe, canonical `prisma migrate` CLI); changed after review because Prisma still has no native PostGIS geometry/geography support as of 2026 ([prisma/prisma#25768](https://github.com/prisma/prisma/issues/25768), still open) and this project has a concrete future use for it — a Places/map view from photo GPS EXIF data fits the Apple Photos reference the editor spec is built around. Drizzle's SQL-first design doesn't block adding that later. | 2026-08-06 (`TASK-job-state-machine.md`) |
| — | NATS JetStream stream/subject layout, first needed by `TASK-nats-job-dispatch.md` (CLAUDE.md §4: "NATS subjects... owned by exactly one side — decide ownership in the scaffold task doc," deferred to whichever task first needed it) | One stream `PLEXUS_JOBS`, `WorkQueuePolicy` retention, two subjects: `plexus.jobs.dispatch` (orchestrator publishes, Go worker pool consumes) and `plexus.jobs.results` (Go worker publishes, orchestrator consumes). Orchestrator owns the stream (creates it at boot); each side self-registers its own durable consumer for the subject it reads. | 2026-08-06 (`TASK-nats-job-dispatch.md`) |
| — | TS NATS client library choice, first needed by `TASK-nats-job-dispatch.md` | `@nats-io/transport-node` + `@nats-io/jetstream` + `@nats-io/nats-core` (v3 scoped packages), not the deprecated `nats` v2 package — see D-10. | 2026-08-06 (`TASK-nats-job-dispatch.md`) |
| D-14 | Video/audio processors via ffmpeg were not implemented in slice 3 (`TASK-builtin-processors.md`) | Implemented in `TASK-video-audio-processors.md` (Phase 1, slice 4): `video.transcode`, `video.compress`, `audio.extract`, `audio.convert`, all shelling out to the `ffmpeg` binary via `os/exec` (no cgo binding, unlike govips). Fixed codec pairs per container/format only — see new `D-15`. Orchestrator's `BUILTIN_PROCESSORS` extended to accept the four new ids. | 2026-08-06 (`TASK-video-audio-processors.md`) |
| D-12 | No CI workflow and no worker `Dockerfile` existed, so nothing installed libvips or ffmpeg in a build/deploy image. | Added `.github/workflows/ci.yml` (two jobs: `orchestrator` — pnpm lint/build/test; `workers` — apt-installs `libvips-dev`/`ffmpeg`/`pkg-config`, then `go vet`/`golangci-lint`/`go test`) and `workers/Dockerfile` (multi-stage: `golang:1.26.5-bookworm` builder + `libvips-dev`, `debian:bookworm-slim` runtime + `libvips42`/`ffmpeg`/`ca-certificates`, non-root user). Image isn't pushed anywhere yet — see new `D-16`. | 2026-08-06 (`TASK-ci-worker-image.md`) |
| V-1 | WebGPU has "~80%+ global browser support as of 2026," solid enough to build the editor preview directly on it, per spec §"Open Questions." | Re-checked against MDN (Baseline: "Limited availability"), caniuse's per-browser table, and the WebGPU standards group's own implementation-status wiki. Chrome/Edge solid (default-on since Chrome 113 on Mac/Windows/ChromeOS); Safari plausibly solid on current OS versions (Safari 26) though caniuse disagrees — see new V-5; **Firefox lacks default support on most platforms today** (Windows/macOS-Apple-Silicon only, both recent; Linux/Android still in development) — a live, current-version mainstream-browser gap, not "older browsers/devices" as the spec framed it. Decision: Canvas2D/WebGL2 fallback preview promoted to a first-class, parallel implementation from the start of Phase 2 preview work, not a rare-case stub. Spec's Open Questions section updated to match. | 2026-08-06 (`TASK-editor-scaffold.md`) |
| — | `create-next-app@latest` writes its own per-package `pnpm-workspace.yaml` (build-script allowlist only, no `packages:` key) into `apps/web/`, which would shadow the root workspace file for any pnpm command run with cwd inside `apps/web` (`TASK-editor-scaffold.md`). | Not deferred work — the nested file was deleted and its `allowBuilds` entries (`sharp`, `unrs-resolver`) folded into the root `pnpm-workspace.yaml`, keeping one workspace definition per CLAUDE.md §4. Recorded so a future `apps/web` re-scaffold doesn't reintroduce it silently. | None — already resolved; kept as a note for discoverability. |
| V-5 | caniuse's per-browser table (checked 2026-08-06) reports "Safari 18.7 ❌ disabled by default" for WebGPU, while the WebGPU standards group's own implementation-status wiki (`github.com/gpuweb/gpuweb/wiki`) reports default-on since Safari 26. Plausibly caniuse lagging Apple's 2025 OS-year version renumbering rather than a real capability gap, but not confirmed against a primary Apple/WebKit source (`TASK-editor-scaffold.md`). | Its trigger was "before the WebGPU/WebGL preview task doc finalizes its feature-detection + fallback-routing logic." `apps/web/src/lib/preview/capabilities.ts` (`TASK-preview-renderer.md`) does runtime capability probing (`await navigator.gpu.requestAdapter()` success/failure) instead of user-agent or version sniffing, so it routes to the correct backend regardless of which Safari version-numbering convention caniuse vs. the gpuweb wiki use — confirmed working end-to-end in a real headless-Chromium run (both the `webgpu` and `webgl2` paths were exercised and rendered correctly). The discrepancy stops being load-bearing for correctness; a primary-source check still has some value for QA-effort planning (how much real traffic exercises the fallback path) but that's a materially lower-consequence question than the one V-5 originally tracked, so it's closed rather than left open for a question this design no longer depends on. | 2026-08-07 (`TASK-preview-renderer.md`) |
| V-10 | Whether libvips' `vips_sharpen` operates only on the L channel of a Lab-like colourspace, or on RGB directly, was unconfirmed against a primary source (`TASK-composite-preview-shaders.md`). | Confirmed directly from `libvips/libvips/convolution/sharpen.c` (`master`, fetched 2026-08-07): `vips_colourspace(in, &t[0], VIPS_INTERPRETATION_LABS, ...)` runs first, band 0 (L) is extracted, sharpened, then recombined with the untouched a/b bands and converted back to the original colourspace. `apps/web/src/lib/preview/color-math.ts`'s `applyUnsharpMask()` default of `'lab-l'` was already correct — no code change needed. | 2026-08-07 (`TASK-recipe-fidelity-drift.md`) |
| V-11 | Whether govips's `Modulate` → `ToColorSpace(LCH)` round trip uses the same D65 white point/gamut-clamping strategy `color-math.ts`'s Lab math assumes was unverified against libvips' own source (`TASK-composite-preview-shaders.md`). | Confirmed directly from `libvips/libvips/colour/XYZ2Lab.c` (`master`, fetched 2026-08-07): `XYZ2Lab.X0/Y0/Z0` default to `VIPS_D65_X0/Y0/Z0`. `color-math.ts`'s D65 assumption was already correct — no code change needed. Gamut-clamping strategy specifically is still not separately documented anywhere primary-sourced, but stopped blocking `V-2`'s drift measurement — any clamp discrepancy shows up directly as measured drift, which `TASK-recipe-fidelity-drift.md` now does for `image.adjustColor` (worst observed: mean ΔE 0.125, max per-channel error 0.75/255). | 2026-08-07 (`TASK-recipe-fidelity-drift.md`) |
| V-9 | `workers/internal/processors/sharpen_test.go`'s only available fixtures (`testdata/images/gradient.jpg`/`gradient.png`) are smooth linear gradients with no edges/high-frequency detail, so `image.sharpen` had no golden-fixture assertion that it actually sharpens anything (`TASK-composite-processors-go.md`). | `testdata/drift/source.png` (`TASK-recipe-fidelity-drift.md`) adds a checkerboard region specifically to give `sharpen` real edges to respond to, and the new `apps/web/src/lib/preview/drift.test.ts` measures the sharpen preview's drift against real govips output there. This is a shared cross-language drift fixture, not a `workers/testdata/images/`-style Go-only golden fixture — `workers/internal/processors/sharpen_test.go` itself still has no dedicated edge-response assertion of its own; a future reader wanting that specifically should add it as separate, narrower work, not assume this closed it. | 2026-08-07 (`TASK-recipe-fidelity-drift.md`) |
| — | **Black-corruption bug, not deferred at any point — found and fixed within `TASK-adjust-color-atan2-zero-fix.md` (2026-08-07).** Real-photo testing on `/editor` showed solid black patches in the live preview, reproducing specifically via the Color composite's Saturation control (worse when Light was also applied), in the sky/highlight region of a real JPEG — visually distinct from the already-fixed `V-2` sharpen-noise symptom above (`Sharpen=0.00` ruled it out) and from `V-11`'s gamut-clamp question (this was a correctness bug, not measured fidelity drift). Root cause: `applyAdjustColor`'s hue step (`color-math.ts`, `ADJUST_COLOR_WGSL` in `webgpu-renderer.ts`, `ADJUST_COLOR_FRAGMENT_SHADER_SOURCE` in `webgl2-renderer.ts`) computed `atan2(lab.b, lab.a)` unconditionally. `Math.atan2(0, 0)` is well-defined in JS (`0`, ECMA-262) — an exhaustive ~17.5k-sample RGB-cube sweep through the TS reference found zero black-output cases — but WGSL's `atan2` spec (`atan(e1/e2)` with quadrant correction) makes `atan2(0,0) = atan(NaN) = NaN`, and GLSL ES's own spec states results are undefined when both arguments are zero. A pixel hits `a==0 && b==0` exactly when perfectly achromatic — routine after an 8-bit `rgba8unorm` texture round-trip quantizes a near-gray sky pixel to exact `r==g==b`. The resulting `NaN` propagates through `cos`/`sin` into the boosted Lab channels, through the RGB conversion matrix, and gets clamped to black on write to the 8-bit output texture — matching every observed symptom (black only in near-achromatic regions; worse with `Light` cranked, since exposure boosting pushes more pixels toward pure-white/achromatic). | Guarded all three implementations behind `chroma > CHROMA_EPSILON` (`1e-4`, Lab units): below threshold, skip `atan2`/`cos`/`sin` and use `a=0, b=0` directly (negligible boosted chroma regardless of hue at that point) — real scalar `if` in WGSL/GLSL, not `select()`, to avoid depending on `select()`'s branch-masking being NaN-clean on every driver. Added a JS-side regression case (`color-math.test.ts`) pinning that a perfectly-achromatic pixel at `saturation>0` returns a finite, non-NaN result equal to the unboosted input — the exact condition that was undefined in WGSL/GLSL, even though JS itself never hit the bug. Lesson for future work in this area (also relevant to `V-2`/`V-9`'s drift harness): the drift harness and this repo's Vitest suite both run in Node/jsdom, so a bug specific to WGSL/GLSL floating-point or undefined-behavior semantics is invisible to them structurally — needs either a real browser/GPU check or cross-referencing the target shading language's own spec, not just testing the TS mirror harder. No Go change — `workers/internal/processors/adjust_color.go` calls govips' `Modulate`, not a hand-rolled `atan2`. | 2026-08-07 (`TASK-adjust-color-atan2-zero-fix.md`) |
| V-2 | How much drift is acceptable between the WebGL/WebGPU live-preview approximation and the Go full-resolution render was undecided — the spec's "Recipe fidelity" success metric (CLAUDE.md §0 Tests) requires a *numeric* bound per composite control, not "close enough." | `TASK-recipe-fidelity-drift.md` built a cross-language measurement harness: `workers/cmd/gendriftfixture` synthesizes a 128×128 fixture (gradient/hue-patches/checkerboard regions) and `workers/cmd/gendriftgolden` runs the real Go processors against it, dumping raw RGBA8 goldens (`testdata/drift/`); `apps/web/src/lib/preview/drift.test.ts` runs `color-math.ts`'s reference math against the same source pixels and asserts measured mean-absolute-error/max-error/mean-ΔE bounds, set ~1.3–1.7× above the actual worst-case point observed per control (not chosen ahead of measurement). Result: Light/Color/B&W are all near bit-exact (worst max error 1.16/255, i.e. sub-1% at 8-bit precision); Sharpen initially showed real divergence (worst max error 87.76/255 at `intensity=1.0` on hard edges) because the TS reference was a plain unsharp mask while libvips' real `vips_sharpen` has cored/threshold response the reference didn't replicate. `image.resize`'s drift (`V-6`) is explicitly out of scope — different mechanism (geometry vs. per-pixel color), its own follow-up. **Real-world symptom observed 2026-08-07** (`/editor`, user report): with Sharpen intensity near 1.0, stacking Light + Saturation on a real (JPEG-noisy) photo produced visible black/white speckling in flat dark regions — the missing cored/threshold response meant the preview's unsharp mask amplified ordinary compression noise as if it were a real edge, and Light/Saturation's own nonlinear passes (exposure gain, Lab round-trip) added just enough extra per-pixel variance beforehand to make it visible. **Fixed 2026-08-07** (`TASK-sharpen-coring-fix.md`): `applyUnsharpMask` (`color-math.ts`) and both shaders (`UNSHARP_WGSL`, `UNSHARP_FRAGMENT_SHADER_SOURCE`) now implement libvips' real piecewise response — `x1=2` (flat/jaggy threshold), `y2=10`/`y3=20` (max brighten/darken clamp), `m1=0` (zero response inside the threshold) — confirmed via govips' own source (`vips/image_pixel.go` v2.18.0: `Sharpen`'s `SharpenOptions` only sets `Sigma`/`X1`/`M2`, so `M1`/`Y2`/`Y3` take libvips' C defaults, not invented values). Drift dropped substantially: Sharpen's worst observed max error is now 38.0/255 (was 87.76/255) at `sharpen-1.0`; `SHARPEN_BOUNDS` tightened from `{ mae: 1.0, max: 110, meanDeltaE: 0.3 }` to `{ mae: 0.55, max: 50, meanDeltaE: 0.17 }` to match. Residual drift is the still-accepted, smaller approximation gap (TS reference vs. libvips' exact cored-response curve), not a bug. | 2026-08-07 (`TASK-recipe-fidelity-drift.md`, coring fix 2026-08-07 via `TASK-sharpen-coring-fix.md`) |
